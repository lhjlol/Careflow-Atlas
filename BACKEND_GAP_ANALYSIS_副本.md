# CareFlow Atlas 后端补全计划与缺口分析

日期：2026-09-13  
范围：单一机构、负责人界面的纸本／Excel 最小共享试点；不做机构／人员数据隔离、手机现场记录或 OCR。

> 已根据 2026-09-13 用户对 D1–D8 的回答修订。Excel 先做候选格式自动识别；D2、D8 暂留空；D3、D6 已确定；D4、D5 保留选项；D7 采用本文制定的试点工程默认值。本次只调整文档，没有实现识别器、训练模型、OCR 或后端。

> **2026-09-13 第二次修订**：纸本／Excel 优先、角色与时间分开、可信导出基线、延期管理页面、不同 checkout 的修改保护已在上一轮写入；本轮把开工前必须说清的四点收进正文，并把起点收窄为一个有边界的 W0。
>
> 1. 新增 §8.1.2「W0 支援范围」：第一版固定少量候选格式，支持表头别名、位置变化和人工修正；每种格式必须有成功、歧义、拒绝识别三类回归样例；仍不训练模型。避免第一期变成通用 Excel 识别平台。
> 2. §8.3 与 A5 的去重承诺加上前提：**有稳定业务键才保证去重；没有时显示疑似重复并要求核对**，内容相同本身不足以判定。
> 3. 新增 §8.2.1「待核对行」：一行待核对不阻塞整批，但必须由负责人显式选择「整批补齐后提交」或「主动拆出有效批次、重新预览后提交」；开发实现不得自行略过问题行；并说明未提交批次 7 天失效后待核对资料如何取回。
> 4. 新增 §7.4「追加更正的生效规则与影响统计」：原事件保留追溯，但更正生效后必须退出覆盖计算，覆盖、历史与跟进改用有效版本，否则错填的较晚日期会继续被当成最新。
> 5. §5、§6.2、§12、§14 明确：D4／D5 未定不阻塞 W0 与契约设计，但真正落实数据库迁移、登录与恢复演练前必须选定一条实作路径；**预留接口不等于同时实现两套**。
> 6. §14 把下一步收窄为「固定候选格式＋映射预览＋回归样例」，验收后再推进共享数据闭环，本轮不再扩写总体规划。
>
> 本次同样只调整本文档，没有实现识别器、训练模型、OCR 或后端。

## 1. 结论：保留现有前端，补共享数据闭环

当前项目不是只有页面的静态原型。它已经有数据模型、Excel 解析、校验、合并、追加式观察历史、本地保存、地图和纸本打印。真正缺少的是：**让多个获得授权的同事共享同一份可信数据，并使导入、更新、导出及恢复有一致的规则。**

第一期优先完成以下闭环：

**列印纸本 → 外出填写 → 中心 Excel 回录 → 预览、确认导入 → 查看覆盖及跟进 → 再导出。**

不建议重写 React 前端、3D 地图或全部领域逻辑，也不建议直接把 `localStorage` 替换成一个整份快照上传接口。正确边界是：前端负责展示和交互，服务端负责授权、权威校验、版本比较、原子提交、审计及持久化。

实施顺序：

1. 以现有及可能的 Excel 格式建立规则识别、候选映射和人工预览；不等待训练集或 D2、D8 才开始技术实现。
2. 按单机构、负责人可见全部业务数据完成共享服务；账号／部署适配保留选择，实际联机发布前再落实 D4、D5。
3. 验证重复导入、多人冲突、故障重试及备份恢复，达到真实数据试点准入条件。
4. 根据实际操作阻塞，再补人员、家庭、居住、会员、布局等管理页面。

权限、事务、幂等和备份在第二步中实现，第三步是验证和加固，不是上线后再补这些保障。

## 2. 分析基线与修改保护

| 项目 | 本次分析依据 |
| --- | --- |
| 仓库 | `OscarXuHz/Careflow-Atlas` |
| checkout | `/Users/lhj/.copilot/repos/copilot-worktrees/Careflow-Atlas/lhjlol-crispy-guacamole` |
| 分支 | `lhjlol-backend-gap-analysis` |
| HEAD | `8127ef33170a1e688463ba121d0fe558a1968245` |
| 首次分析时的未提交状态 | 未跟踪的 `BACKEND_GAP_ANALYSIS_副本.md`、`plan.md`、`plan_副本.md`；原 `BACKEND_GAP_ANALYSIS.md` 已不在磁盘 |
| 首次分析的交付 | 按用户确认更新该副本并同步 `plan.md`；不重建原文件，不改 `plan_副本.md` 或业务代码 |
| 本次修订的工作目录 | `/Users/lhj/Desktop/Light/洗楼系统/Careflow-Atlas`（不是上表的 checkout） |
| 本次修订的分支／HEAD | `main`、`8127ef3`（与首次分析同一提交） |
| 本次修订前的工作树 | `git status` 只有三个未跟踪文件：本副本、`CareFlow_紙本回錄_mock範本.xlsx`、`CareFlow_街區擴展_mock.xlsx`；该目录没有 `plan.md`、`plan_副本.md` 或 `BACKEND_GAP_ANALYSIS.md`，也没有已修改的受跟踪文件 |
| 本次修订的交付 | 只改本副本；不新建 `plan.md`，不改业务代码、依赖或资源 |

用户反馈另有四个未提交文件涉及镜头防遮挡修复，但当前 checkout 未显示这些改动。反馈中的 `/Users/jiahangx/Projects/Careflow-Atlas` 不是本次工作目录。因此本文不宣称审阅过那四个文件，也不把当前工作树称为干净。后续开发前应重新确认相关 diff 的归属，不能覆盖、还原或顺带格式化镜头修复。

本次修订所在的工作目录同样没有显示镜头防遮挡相关的已修改文件（见上表「本次修订前的工作树」），该结论只针对本次修订时的那一棵工作树，不代表其他 checkout 的状态。两次分析位于不同 checkout、同一 HEAD，改动前仍应按上述要求确认 diff 归属。

本文依据当前仓库源代码及仓库文档进行静态分析，没有拿到真实机构纸本／Excel、CareFlow 主系统接口或生产部署配置。既有测试文件是设计参考，不代表本文已经验证多人后端、设备性能或真实数据兼容性。

## 3. 目录盘点：已有能力与补充位置

| 目录／文件 | 已有能力 | 补充方向与边界 |
| --- | --- | --- |
| [src/app/App.tsx](src/app/App.tsx) | 样例加载、Excel 预览、保存、导出、地图和详情组合 | 接入会话与异步 repository，等待服务器提交后显示成功；正式模式不再加载演示数据 |
| [src/app/store.ts](src/app/store.ts) | Zustand 保存全量 snapshot 和建筑／楼层／单位选择 | 注入本地／远程实现，增加请求状态、重试和缓存失效；保留选择与镜头交互状态 |
| [src/data/repository.ts](src/data/repository.ts) | 同步 localStorage 适配器、整份快照校验和保存、追加观察 | 新增异步远程 repository；正式写入改为具体业务命令，不向普通用户开放整份快照替换 |
| [src/domain/types.ts](src/domain/types.ts)、[schema.ts](src/domain/schema.ts) | 十类实体集合、引用校验、覆盖状态和跟进推导 | 提取可共享规则；新建正式数据契约，保留演示标记和旧版本兼容边界 |
| [src/data/workbookImport.ts](src/data/workbookImport.ts) | 旧英文合成格式解析、行级问题、文件和行列限制 | 保留为演示适配器；不要冒充真实机构 Excel 的通用导入器 |
| [src/data/workflowWorkbook.ts](src/data/workflowWorkbook.ts)、[workflowMerge.ts](src/data/workflowMerge.ts) | 中文六表导入导出、封存基线、保守合并和历史保护 | 拆分普通机构表与系统回录表；在服务端建立可信基线、导入任务和事务 |
| [src/data/workflowFormat.ts](src/data/workflowFormat.ts) | 中文表头、香港时间格式化、汇总及完整封存 | 复用展示规则，生成前按权限裁剪数据；正式格式需单独版本化 |
| [src/components/ImportDialog.tsx](src/components/ImportDialog.tsx) | 预览、问题列表、确认等待及错误显示 | 接服务端任务，增加过期、冲突、重新预览和恢复提交状态 |
| [src/components/PaperForm.tsx](src/components/PaperForm.tsx) | 建筑级 A4 预览及浏览器打印 | 使用已授权的打印数据；无需为第一期强制开发 PDF 服务 |
| [src/components/BuildingDetail.tsx](src/components/BuildingDetail.tsx)、[ObservationHistory.tsx](src/components/ObservationHistory.tsx) | 建筑／单位历史、楼层选择、覆盖与跟进 | 适配按范围读取，区分楼层历史；独立楼层创建页面不是默认第一期任务 |
| [src/components/ObservationEditor.tsx](src/components/ObservationEditor.tsx) | 中心补录表单、保存等待、失败保留表单内容 | 若正式模式保留入口，必须走同一授权和事务路径；不扩展成手机现场记录 |
| [src/map/](src/map/) | MapLibre 地图、独立空间模型、楼层展示、失败降级 | 保留现有渲染及镜头实现；只对接授权后的几何和摘要，不传人员及自由文本 |
| [src/app/webmcp.ts](src/app/webmcp.ts) | 可选的浏览器摘要／导航工具 | 不是后端；正式环境默认是否开放另行决定，不能绕过数据范围 |
| [src/data/](src/data/) 中 demo 和 geometry 资源 | 合成业务样例及底图轮廓来源 | 与正式数据隔离；没有登记、政府数据或地址真实性保证 |
| [public/demo/](public/demo/) | 三份预制演示 XLSX | 可以继续支持演示；正式导入文件、导出文件和备份不得放到公开静态目录 |
| [scripts/](scripts/) | 旧样例生成、街区样例生成及几何提取 | 属于开发工具，不是后台业务服务；可复现性问题见第 11 节 |
| [package.json](package.json)、[vite.config.ts](vite.config.ts)、[tsconfig.json](tsconfig.json) | 前端开发、构建及 Vitest；没有服务端启动和数据库迁移脚本 | 选定独立部署或主系统集成后，再补相应服务端构建、迁移和代理配置 |
| [.env.example](.env.example)、[.openai/hosting.json](.openai/hosting.json) | 公共地图配置和 `dist` 静态发布 | 补服务器配置、数据服务和运维边界；静态站点访问限制不等于业务权限 |
| [docs/](docs/) | 架构、演示边界、Excel 流程和历史验证记录 | 实施时更新对应文档，不把旧验证记录或候选业务规则当成生产事实 |
| `src/**/*.test.ts` | 12 个文件覆盖 schema、repository、Excel、状态和地图模型 | 保留已有语义用例；后续增加服务端事务、授权、导入合同和闭环验证 |

当前实际数据流：

```text
浏览器读取 XLSX / 中心补录表单
  -> 浏览器解析、校验、预览、合并
  -> OutreachRepository
  -> localStorage 中的完整 snapshot
  -> Zustand
  -> 详情、历史、覆盖、纸本、地图
  -> 浏览器导出 XLSX / JSON
```

应用中的 `fetch` 用于下载演示工作簿，地图另外访问外部底图资源；仓库中没有支撑上述业务读写的 HTTP API 服务。

## 4. 第一期范围与明确延期项

| 类别 | 纳入内容 | 不应顺带扩展的内容 |
| --- | --- | --- |
| 当前输入约定 | 先支持候选 Excel 格式识别；保留未知身份和未知来源；账号部署留适配边界 | 不等待完整训练集，不擅自补齐 D2 或 D8 |
| 必需：共享访问 | 负责人登录后可查看全机构业务数据；保留登录、会话及操作审计 | 不做租户、部门、工作员归属的行级隔离或复杂角色平台 |
| 必需：纸本工作 | 现有建筑、布局及跟进的打印；无资料时显示空状态 | D8 暂空，不自动选定初始资料来源或创建演示建筑冒充正式资料 |
| 必需：Excel 导入 | 表头／工作表／值类型自动识别、候选映射、人工确认、事务和幂等；有业务键才保证去重，无业务键只提示疑似重复；后续替换识别器 | 不把格式识别当作身份匹配或自动批准入库；第一版只支持第 8.1.2 节的固定候选格式，不承诺识别任意 Excel |
| 必需：查看与继续工作 | 共享覆盖、历史、待跟进、再次导出 | 不将示范覆盖比例包装为机构 KPI |
| 必需：资料保护 | 服务端授权、来源审计、受控导出、备份和恢复、基本监控 | 不把普通用户导出完整 JSON 当作备份制度 |
| 按需延期 | 人员、家庭、居住、会员、布局的管理页面；独立楼层补录；精细报表 | 不因模型存在就默认实现全套 CRUD |
| 后续产品能力 | 确认需要后再做提醒、多级审批、多机构运营管理、大规模队列 | 不成为最小试点的默认依赖 |
| 明确排除 | OCR／纸本图像识别、手机现场记录、离线同步、自动医疗／法律判断、未经确认的政府数据集成 | OCR 是未来可能方向，当前不接收图片进行文字识别 |

延期管理页面不代表可以丢弃现有关系。D8 的正式初始化和维护来源暂留空，不默认选择导入、手工建档或主系统同步。可以先用合成资料验证闭环；正式库没有可靠目标时，待匹配行留在预览中，不造楼层、坐标或人员身份。

## 5. D1–D8 的当前决定

| 编号 | 用户回答／状态 | 本轮采用的方案 | 当前边界 |
| --- | --- | --- | --- |
| D1 | 参照可能格式自动识别，后续有训练集再改 | 先做规则驱动识别和可替换接口，自动生成候选映射；负责人预览确认 | 训练集尚未提供，不宣称已训练或兼容所有机构表；识别不自动写库 |
| D2 | 先空置，纸本未来可能做 OCR，现在不做 | 纸本角色及未知原始时间保留可空字段，不设必填，不推断 | 不做 OCR；上传／确认账号和服务端时间仍由系统记录，不替代空缺纸本信息 |
| D3 | 单一机构，暂不做数据隔离 | 一套机构数据，当前不建多租户或按人员／部门隔离 | 不等于允许匿名访问；正式与 demo 数据仍分开 |
| D4 | 未确定，保留选择 | 主系统账号接入或独立登录保留两种实现边界 | 可先用合成会话适配测试；正式访问不能长期跳过登录。**最迟决策点：W2 接入真实登录之前选定**，之后只维护一条可运行路径 |
| D5 | 未确定，保留选择 | 共用主系统部署或独立服务部署均保留 | 不提前安装某种数据库／云平台；**最迟决策点：W2 执行第一次真实数据库迁移之前选定**，W6 恢复演练必须在已选定环境上做，不能只在适配层上宣称完成 |
| D6 | 面向负责人，可看到所有人的数据 | 负责人界面拥有全机构业务数据可见性，不按上传者过滤同事数据 | 默认闭环提供打印、导入确认和业务导出；数据库恢复仍走受控运维，不因“可见全部”暴露备份凭证 |
| D7 | 由本方案处理 | 第 11.3 节给出明确的原文件、导出、审计、备份、恢复和删除默认策略 | 属于试点工程基线，不宣称替代适用的机构／法律义务 |
| D8 | 还不知道，先空着 | 初始建筑／布局来源和普通 Excel 主数据更新策略：暂留空 | 不阻塞识别器、空状态、服务框架及合成验证；真实资料初始化仍待后续补充 |

负责人默认可预览和确认同一批次，不加入独立复核或多级审批；多位负责人可以接续处理同机构批次，记录各自操作即可，不按批次创建者私有化数据。账号是否由主系统提供仍属 D4 的未定选项。

**D4／D5 的「保留选择」只到接口为止。** 未定不阻塞 W0 与 W1 的契约设计，但真正落实数据库迁移、登录与恢复演练前必须先选定一条实作路径并记录决策（见第 12 节的前置条件）。预留适配接口不等于同时实现两套，也不等于可以无限期推迟选择。

没有稳定 ID 或目标版本的普通 Excel，自动识别也只能给出字段／记录候选，不能自动覆盖已有主数据。未知值留空、原单元格留存；需要业务必填值的行进入待核对。D2 可空字段本身不阻止导入，但缺失业务日期的观察不得凭服务器时间加入覆盖计算，应保留为待补充行。

## 6. 候选架构与新增代码位置

### 6.1 架构选择

如果 CareFlow 主系统已有合适的身份、数据服务和运维能力，优先将外展模块接入其既有边界，而不是再造账号和数据库体系。如果需要独立后端，可采用一个 TypeScript 服务和关系数据库；PostgreSQL 是候选，不是已批准或已安装的依赖。

```text
React / Zustand / 打印 / 地图
  -> 异步 HTTP repository
  -> 已认证的业务 API
       -> 授权、校验、查询、导入确认、受控导出
       -> 关系数据库：业务记录、版本、导入批次、审计
       -> 受控文件存储：按政策保存的原文件及临时导出

演示模式 -> 原有本地 repository（与正式数据隔离）
```

第一期采用同一业务服务即可。文件体量在确认范围内时，不要求独立消息队列；若资源受限解析不能在合理请求时间内完成，再将已有导入任务处理移至受限工作进程。任务进度不是引入 Redis 或微服务的理由。

本期已经确定为单机构、负责人全量可见，不创建租户注册、机构切换、按人员归属过滤或行级隔离平台。服务端只需可靠识别负责人会话和必要的业务／运维操作边界；查询按建筑分页是业务筛选和性能控制，不是把同事的数据隐藏。未来若改为多机构，另作 schema／授权迁移，不提前建设。

### 6.2 建议新增文件

以下路径均为**尚不存在的候选位置**，用于明确代码职责。若接入主系统，应映射到主系统已有目录，而不是机械创建另一套服务。

| 建议位置 | 需要实现的内容 | 所属工作包 |
| --- | --- | --- |
| `shared/contracts/` | 正式请求／响应 schema、来源／时间结构、错误与分页契约；与 demo schema 分开 | W1 |
| `shared/domain/` | 无浏览器依赖的覆盖、跟进、关系约束；迁移已有规则而非复制两份不同逻辑 | W1 |
| `server/src/app.ts`、`config.ts` | 服务启动、必要环境变量校验、结构化错误、健康检查 | W2 |
| `server/src/auth/` | 主系统／独立身份适配接口、负责人会话及业务／运维操作检查；无人员数据隔离 | W2 |
| `server/src/db/`、`server/migrations/` | 连接、迁移、外键／唯一约束、事务；D8 初始化入口暂留空 | W2 |
| `server/src/outreach/queries.ts` | 建筑摘要、单幢详情、范围历史、待跟进和打印数据查询 | W3 |
| `server/src/outreach/commands.ts` | 受控新增观察、结案等共用命令；供导入使用，中心补录入口保留时也复用 | W4 |
| `server/src/imports/detectors/` | 候选格式、表头行和字段识别、解释及歧义输出；预留训练模型适配器 | W0、W4 |
| `server/src/imports/profiles/` | 版本化表头别名、候选格式、字段类型和必需关系，不夹带训练模型实现 | W0、W4 |
| `server/src/imports/parsers/` | 按已确认映射解析普通表／系统回录表；只处理 XLSX，不做 OCR | W4 |
| `server/src/imports/service.ts` | 批次状态、预览、目标匹配、版本绑定、事务确认、重试结果查询 | W4 |
| `server/src/exports/service.ts` | 授权范围内导出、可信基线记录、到期及下载时重新授权 | W5 |
| `server/src/audit/` | 上传、确认、导出、更正等操作记录；不在通用日志写完整个人资料 | W2–W5 |
| `src/data/remoteRepository.ts` | API 调用、错误转换、幂等键、取消过时读取；不静默退回本地正式写入 | W3–W5 |
| `src/app/session.ts`、`src/components/SessionGate.tsx` | 当前会话、未登录／权限不足显示、重新认证；有主系统组件则复用 | W3 |
| `server/tests/` 或主系统测试目录 | 真实数据库约束、授权、并发、契约和恢复相关集成用例 | W2–W6 |
| `server/tsconfig.json`、服务端包脚本及部署配置 | 仅独立服务方案需要；与当前浏览器编译分离 | W2、W6 |

不在第一期默认新增通用人员、家庭、会员管理控制器或页面。基础关系的导入和只读使用并不依赖这些完整管理界面。

上表里的 D4／D5 双路径只体现在**接口与配置边界**上，各保留一份适配接口、可替换的 session 来源和可替换的 repository 实现。同一时间只维护一条可运行路径，另一条只保留接口定义与最小冒烟验证；正式迁移、登录接入或恢复演练开始前必须先选定，不能把两套实现都留在仓库里当作「以后再说」。

## 7. 数据结构与必须维持的约束

### 7.1 现有实体如何保留

候选关系数据库可按以下实体组织。表名仅为建议，不等于要求十张表或十套管理模块全部独立实现。

| 当前集合 | 需要保留的职责 | 关键约束 |
| --- | --- | --- |
| `buildings` | 建筑身份、名称、地址、坐标和可选轮廓 | 名称／同址不是唯一身份；轮廓和坐标来源需要核对 |
| `floors`、`units` | 已明确的楼层和单位布局 | 单位所属楼层与建筑一致；不得根据备注凭空制造布局 |
| `people` | 人员称呼、电话文字和必要备注 | 同名、同电话、同址不自动去重；按权限查询和导出 |
| `households`、`householdMemberships` | 家庭及成员关系 | 人员不是家庭；多人同址不代表一个家庭 |
| `householdResidences` | 随时间变化的居住关系 | 起止日期和建筑／单位引用有效；搬迁不删除旧探访 |
| `memberships` | 会员状态及有效期记录 | 不根据一次接触自动判断资格，不把 Excel 参考列当作审批命令 |
| `visits` | 实际外出批次及来源 | 可涉及多幢建筑；编号不由浏览器启动周期定义 |
| `observations` | 位置、结果、来源和时间明确的历史事件 | 原则上追加；范围引用一致；覆盖、接触、住房判断保持独立 |

不要直接将 `schemaVersion: "0.1-demo"` 作为正式数据库版本。新建正式 schema 和显式迁移；保留原演示校验。`alignDemoSnapshot` 是已知演示几何迁移，不是正式地址纠正或地理编码工具。

### 7.2 新增的服务端记录

| 候选记录 | 最小字段职责 | 约束 |
| --- | --- | --- |
| 身份／授权映射 | 身份主体、负责人访问及必要运维操作 | 可复用主系统；当前不加租户或部门／人员行级规则，不接受客户端自称负责人 |
| `import_batches` | 来源文件摘要、识别器／格式／映射版本、上传者、预览版本、状态、提交结果 | 确认结果与批次原子保存；当前同机构负责人可接续处理，不仅限创建者 |
| `import_rows` 或等效批次明细 | 原行定位、规范化内容、目标 ID、匹配决定、目标版本、问题 | 未解决的匹配不能进入提交；内容按保留政策保存 |
| `export_revisions` | 导出范围、实体及行版本、必要的可信基线内容、导出者和期限 | 工作簿携带不透明引用；服务器保存足够的基线以进行比较，不能只存不可比较的任意编号 |
| `audit_events` | 谁在何时对哪些记录执行了什么操作及关联批次 | 追加记录，访问受限；仅保存审计必需内容，原始个人资料按政策控制 |
| `idempotency_records` 或等效唯一约束 | 请求键、认证范围、业务载荷摘要及已提交结果 | 相同键相同请求返回已有结果；不同内容复用同键报冲突 |

现有跟进可继续由 observation 的 `followUp` 和 `resolvesObservationId` 表达，无需第一期就拆出完整任务管理系统。建议在数据库中为非空结案引用设置唯一约束，并在事务内校验被结案事件确实开放、位置一致；不能只在前端检查一次。

获准的主数据更新应使用行版本或等效乐观锁；D8 未定时不开放普通 Excel 无基线覆盖。观察事件保持不可覆盖；更正采用带原因和原事件引用的追加记录。**只追加一条更正记录、却仍让原事件参与最新值比较，等于更正没有生效**；有效版本与影响统计见第 7.4 节。删除／脱敏按第 11.3 节默认流程执行，不能把“历史追加”理解为个人数据永不删除。

### 7.3 角色与时间：不能用登录者替代工作员

**D2 当前留空。** 下表是可选字段预留，不要求本期完成纸本身份建模或录入界面。已有原值保留，未提供的纸本工作员、回录者及原时间为 `null`／缺省；不从姓名推断账号、不用登录者补空白。不接 OCR、照片或扫描件识别。

| 信息 | 候选字段 | 数据来源 |
| --- | --- | --- |
| 现场工作员 | `fieldWorkers`：原始姓名及可选已核对 ID | 纸本／原 Excel；允许没有系统账号或尚未匹配 |
| 回录者 | `enteredByRaw`、可选核对身份、`enteredAtRaw` | 原表或回录流程声明；缺失时保持未知，不伪装成认证身份 |
| 上传者 | `uploadedByUserId`、`receivedAt` | 当前认证会话及服务端时间 |
| 确认者 | `confirmedByUserId`、`committedAt` | 确认时认证会话及事务提交记录 |
| 实际探访时间 | 日期，或带时区的时刻，以及精度标记 | 经契约规范化的纸本／Excel 值 |
| 原始时间 | `occurredAtRaw`、原日期／原时间／时区依据 | 保留原值，不用推测补造时分秒 |
| 纸本和文件来源 | 纸本编号／行号、外出编号、文件名／摘要、sheet／row、批次、格式／映射版本 | 原表定位及系统生成的处理信息 |

如果原表有“甲探访、乙回录”，保留原值；如果没有，留空，不将这两类信息设为本期必填。服务器仍记录实际上传和确认的负责人及时间；可以同人操作，也可由另一负责人接续。完整纸本角色关系与未来 OCR 待后续补充。

旧记录的 `workerName`、`recordedAt` 保留为 legacy 值，并注明原语义。不得将旧录入时间强行转换成服务端接收时间，也不得回填并不存在的认证主体。只填日期的探访保持日期精度；相对时间保留原话，确认之前不生成期限。

### 7.4 追加更正的生效规则与影响统计

纸本回录最常见的更正是「把错填的日期或结果改回来」，所以更正不能只是多一条历史记录：必须定义哪一条是**有效版本**，否则更正不会真正改变覆盖、跟进和导出结果。

**记录方式：追加，不改写原事件**

| 字段 | 职责 |
| --- | --- |
| `correctsObservationId` | 指向被更正的原观察事件；必填，构成更正链 |
| `correctionReason` | 更正原因（错填日期、位置、结果、笔误等），供追溯，不参与覆盖计算 |
| `correctedByUserId`、`correctedAt` | 更正者与提交时间，取自认证会话 |
| 事件字段 | 与普通观察同一组：位置、`occurredAt`（更正后的实际发生时间）、覆盖、评估、接触结果、证据、备注、跟进 |
| `supersededByObservationId` | 由服务端推导，不接受客户端写入，用于标记原事件被哪条更正取代 |

约束：只能指向同一机构内已存在的观察事件；不得指向自身或形成环；跨建筑、跨单位或在预览外指向要在确认时提示并要求核对。原事件的任何字段都不改写、不删除。

**有效版本解析（W1 定义契约，W4 实现）**

1. 先排除**已被更正取代**的事件：任何被至少一条更正记录通过 `correctsObservationId` 指向的事件退出覆盖／摘要候选集，但完整保留在历史中。
2. 更正记录本身作为一条新的观察事件参与比较，按它自己的 `occurredAt`（更正后的实际发生时间）与 `correctedAt` 排序。这正是为了避免：原事件错填了较晚的未来日期，只追加一条更正后，错误事件仍因发生时间较晚而被当成最新。
3. 同一原事件被再次更正时形成更正链，**链尾生效**；链上每一环都可在历史中追溯。
4. 两条更正并列指向同一原事件、彼此没有先后关系时报 `409` 冲突，要求人工判定保留哪一条；不得按写入时间静默取最后一条。

这需要替换现在「按发生时间、录入时间取最新」的比较规则（[types.ts:74](src/domain/types.ts#L74) 的 `compareObservationTime`／`latestObservation`，以及基于它的 [getCoverageStatus](src/domain/types.ts#L81)、[getCoverageSummary](src/domain/types.ts#L91)）。在 W1 定义、W4 落地之前，不能对外宣称更正已经生效。

**对覆盖、历史与跟进的影响**

| 面向 | 采用有效版本后的行为 |
| --- | --- |
| 覆盖与摘要 | 用「排除已取代事件」后的集合重算；更正使某单位从已完成退回未完成（或相反）时必须如实反映，并提升受影响范围的版本 |
| 历史 | 按时间列出原事件与全部更正，标注「已更正／已取代」与「当前有效」；追溯视图不隐藏原事件，也不把原事件混进当前状态 |
| 跟进 | 以有效版本携带的跟进为准；更正导致原跟进消失或到期日变化时，必须产生一条显式的跟进变更记录（原因、旧值、新值），不得让待跟进项目静默消失 |
| 已关闭的跟进 | 引用被更正事件的结案记录仍作为历史保留；不因更正自动重开或自动关闭，需人工确认后再追加相应记录 |
| 统计与导出 | 更正触发受影响范围的摘要重算；导出与可信基线携带更正链摘要，接收方能看到「哪些行的当前状态来自更正」 |
| 审计 | 原事件、更正、判定结果都进审计；不复制整份原表或整条观察原文 |

**必须写进验收的情形（A19）**：把错填的未来日期更正为较早日期后，覆盖与摘要必须按更正后的日期计算，原错误事件不得继续被当作最新；历史中两条都能看到，并明确标出哪一条是当前有效版本。

## 8. Excel 导入导出：第一期的核心业务服务

### 8.1 两条输入路径

| 路径 | 输入约定 | 更新和基线规则 |
| --- | --- | --- |
| 机构普通 Excel | 先由候选格式／规则识别，负责人确认字段映射；不要求「關聯封存」 | 字段识别与实体匹配分开；D8 更新策略未定，无稳定身份／基线则不覆盖 |
| 系统导出后回录 | 格式版本、稳定记录标识、服务端导出引用及允许编辑的列 | 服务端用可信导出基线识别未改行、可应用修改及冲突；文件自带 JSON 不具备权威性 |

当前中文 parser 会要求完整封存，并从中恢复所有实体。这只能继续作为演示格式兼容路径；正式普通 Excel 需要独立适配器。旧英文导入器同样是特定合成格式，不应直接改名为机构导入器。

本期负责人可以查看和导出全机构业务数据；选择某栋楼或部分记录导出时，仍应让全部页／封存遵守所选输出范围，不能声称“只导出这栋楼”却附带整个机构。系统凭证、运维密钥和无关原始文件不属于业务数据，不进入导出。

### 8.1.1 自动识别第一版：规则先行，训练集后接

1. 以现有中文六表、旧英文格式，以及可能的“一人一行”“一幢建筑一行”“一处一次探访一行”作为候选 profile；后几种是推测模板，必须在预览中标识，不宣称机构原表已经验证。
2. 在受限解析范围内，结合工作表名、前若干行的表头、常见中英文／繁简别名、空白和标点归一化、列值类型及必需字段组合，识别表头位置、表类型和候选字段。例如“姓名／称呼”“電話／联系电话”“大廈／建筑”可列别名，但相似列名不直接决定业务含义。
3. 输出 `detectorVersion`、候选 `profileId`、候选表头行、字段映射、匹配依据及 `KNOWN / CANDIDATE / AMBIGUOUS / UNKNOWN` 状态。规则得分不是经过校准的成功概率，不给虚假准确率承诺。
4. 已知格式且没有关键歧义时自动填入预览；候选或多义字段要求负责人选择／修正。未知非空列、重复列名、缺少关键业务字段均明确提示，不能静默丢弃。D2 的可空角色字段除外。
5. 确认映射后再运行 schema、引用、日期、历史和冲突校验，最后仍须点击确认入库。`detector` 只建议“这列是什么”，不判断“这个人是谁”，不自动新建家庭、楼层、坐标或改写旧记录。
6. 原单元格和负责人修正结果随批次按第 11.3 节短期保留；可作为未来标注参考，但不得自动把真实人员文件变成训练集。后续训练集需明确授权和去标识，独立管理来源、标签、版本和保留期。
7. 固定识别器输入／输出接口与回归样例，之后可增加训练模型适配器。记录模型／规则版本，使用独立评估样本衡量误识别、拒绝识别及字段映射准确性；识别器升级不能改变已确认批次或旧幂等结果。

本期不训练模型、不调用外部 AI 上传机构文件，也不做 OCR。PDF、照片和扫描件给出“暂不支持，请使用 Excel”的明确提示；Excel 中可读单元格才是当前输入。

### 8.1.2 W0 支援范围：第一版固定候选格式，不做通用识别平台

W0 需要一个明确的支援边界，否则「自动识别 Excel」会无上限扩张。第一版只承诺下列清单内的少量候选格式，支持表头别名、位置变化和人工修正；**不以「支持更多格式」作为验收方向**。

| 候选格式 | 来源与状态 | 第一版要求 |
| --- | --- | --- |
| 中文六表封存格式 | 仓库现有格式（[workflowWorkbook.ts](src/data/workflowWorkbook.ts)），已实现 | 保持兼容；识别失败时回退到现有解析器 |
| 旧英文合成格式 | 仓库现有格式（[workbookImport.ts](src/data/workbookImport.ts)） | 保持兼容；标注为演示格式，不宣称为机构原表 |
| 一人一行 | 文档提到的候选模板，**未取得机构原表验证** | 识别表头别名与关键字段；预览中显式标注「推测格式」 |
| 一幢建筑一行 | 同上，推测模板 | 同上；不因一行含多个值而擅自拆分出人员或单位身份 |
| 一处一次探访一行 | 同上，推测模板 | 同上；行与事件的对应关系须在预览中说明 |

**支持的差异范围（只有这四类，超出即拒绝识别）**：表头不在首行、前面有标题或说明行；列顺序不同；列名使用常见中英文／繁简别名；中间存在空行、空列，以及合并单元格造成的空表头。

**明确不支持的**：多级表头、同一工作表混排多种表类型、公式求值、从图表或图片中取数、跨文件合并语义、多文件一次导入。

每种格式必须有三类回归样例，缺一类不算完成：

| 样例类型 | 期望结果 |
| --- | --- |
| 成功 | 自动给出候选格式与完整映射，无关键歧义，可直接进入预览确认 |
| 歧义 | 输出 `AMBIGUOUS` 并要求负责人选择或修正，不自动入库、不猜默认值 |
| 拒绝识别 | 明确给出「不支持／无法识别」及原因，并提供人工映射或退回路径，不伪装成功 |

样例为合成资料，随代码提交、可反复运行，构成 W0 的回归基线（见第 14 节）。

边界保持不变：不训练模型、不调用外部 AI 上传机构文件、不做 OCR。识别器只回答「这列是什么」，不回答「这个人是谁」，也不自动新建家庭、楼层、坐标或改写旧记录。

### 8.2 导入状态与事务

建议状态：`RECEIVED -> VALIDATING -> READY / NEEDS_REVIEW / INVALID -> COMMITTED`；另有 `EXPIRED` 或 `CANCELLED`。这些状态不要求第一期使用异步队列，但必须能在刷新或超时后查询。

1. **接收**：认证并检查上传权限；应用文件与解压资源上限，记录批次和服务端接收时间。原文件的存储位置和期限受 D7 约束。
2. **解析**：运行版本化识别器并返回候选映射，负责人确认后按该版本解析，输出 `{sheet, row, field, code, message}` 问题。不以默认坐标、默认日期或随机构造身份补齐；不执行 OCR。
3. **匹配与预览**：解析结果在服务器与当前授权数据比较；保存目标 ID、行版本、规范化内容和预览版本。普通 Excel 的人工匹配只解决身份，不顺便批准覆盖全部字段。
4. **确认**：提交批次 ID、预览版本和幂等键，而不是任意完整快照。重新校验会话、权限、目标版本、跨记录引用和唯一结案。
5. **原子写入**：在同一事务中写入新增／获准更新的数据、审计、去重记录及批次提交结果。有错误则不产生部分业务导入。
6. **返回与重试**：返回已提交结果及受影响建筑。网络超时后先查询状态，或用同一个幂等键重试；如果已经提交，返回同一结果，不再次生成 observation。

预览后出现并发修改，应产生明确冲突并进入重新预览／核对流程。批次内容、目标匹配或映射变化也必须产生新预览版本，旧确认不能继续提交。

需要允许修改名称、电话等主数据时，采用可信基线比较：与基线相同的旧值不覆盖较新服务端值；服务端未变且客户端获准修改的字段可以应用；双方都变且不一致则报冲突。可采用更保守的整行冲突规则，但必须在契约中明确。

### 8.2.1 一行待核对，是否阻塞整份 Excel

「缺业务必填值（例如日期或目标）的行进入待核对」与「整批原子提交」并不矛盾，前提是把**谁来拆批**定清楚：拆批是负责人的显式决定，不是实现的默认行为，也不能让开发者自行决定略过问题行。

行级分类（预览中可见、可筛选）：`READY` 可提交、`NEEDS_REVIEW` 待核对（缺业务必填值或有未解决的匹配／疑似重复）、`INVALID` 不可提交（结构或引用错误）。

预览页必须让负责人明确选择两条路径之一：

1. **补齐后整批提交（默认）**：负责人补齐或修正待核对行，重新解析产生新的预览版本，再整批提交。只要存在 `INVALID` 行，这条路径不可用。
2. **负责人主动拆出有效批次**：负责人显式选择「只提交可提交行」，系统把当前 `READY` 行组成一个新的预览版本，要求再次确认后才提交；被拆出的 `NEEDS_REVIEW`／`INVALID` 行不进入本次事务，仍留在原批次中并保留各自状态。

禁止的行为：实现自行跳过问题行、用默认日期或默认目标补齐、把部分写入包装成整批成功。拆分提交同样写审计（谁在何时拆出、拆了哪些行），并保留被拆出行的归属批次，方便负责人回头补齐后再次提交。

**批次 7 天失效后，待核对资料怎么取回**（对应第 11.3 节的默认保留期）：

| 情况 | 可取回方式 |
| --- | --- |
| 原文件仍在 30 天保留期内 | 用同一文件重新上传、重新预览。已有稳定业务键的行必须按第 8.3 节识别为重复，而不是再次新增；此前已提交的部分不受影响 |
| 原文件已到期清理 | 系统不再保留原表内容，需负责人重新提供原文件。这是保留政策的直接结果，不算系统故障，也不得用旧预览缓存或已下载的导出文件冒充原表 |
| 已提交的部分 | 不受批次失效影响，始终可在覆盖、历史与导出中查到 |

预览页与批次页必须显示失效时间与剩余时间。批次失效前，负责人可导出**待核对清单**（授权会话内生成、记录审计的受控导出，只含核对所需字段），便于线下先核对再重新上传；该清单按第 8.4 节管理，不得放入 `public/` 等公开静态目录，也不替代原表。

### 8.3 重复导入不能只看文件名：去重承诺的前提

去重能力取决于业务键是否可靠，这一点必须写进契约，不能把「不重复」当成无条件承诺。

| 业务键情况 | 系统承诺 | 系统不得做的事 |
| --- | --- | --- |
| 有稳定业务键：命名空间内的稳定记录 ID，或机构确认唯一的纸本编号／行号／位置组合（现有回录流程已按此生成标识） | **保证去重**：改名、排序、重新上传都不新增重复事件；同键内容一致视为重复；同键内容被改写报 `409` 冲突 | 不得静默覆盖较新内容；不得把不同业务键的两条记录当作同一次探访合并 |
| 没有可靠业务键（普通机构 Excel 的常见情形） | **只显示疑似重复并要求核对**：把规范化内容相同或高度相近的行标为一组，由负责人在预览中逐组判断 | 不得仅凭内容相同自动去重或自动合并，也不得仅凭内容相同就断定是两次不同探访而直接新增 |

内容相同既不足以判定「同一次探访」，也不足以判定「两次合法探访」。疑似重复的判定结果要作为核对记录保留（谁在何时判定为同一次／不同次、依据是什么），它是后续重传时的去重依据；未核对的疑似重复行按第 8.2.1 节进入 `NEEDS_REVIEW`，不自动入库。

文件摘要用于识别完全相同的文件；规范化内容用于同一业务键下的一致性判断；两者都不能替代业务身份。服务端接收时间、批次 ID、文件名等处理元数据不参与「业务内容相同」的判断，否则每次重试都会变成不同事件；原始 provenance 仍需单独保留。

### 8.4 导出与打印

普通业务导出先确认负责人会话及所选导出范围，再生成全部工作表、封存、JSON 和元数据；本期不按人员归属屏蔽数据。全机构导出明确标注为全量，单幢导出不夹带机构全量封存。导出记录与可信基线绑定，保留格式版本和必要的编辑说明。

下载时重新检查权限、文件归属和期限；权限收窄后使原下载失效并要求重新生成。已合法下载到用户设备的离线副本无法通过服务器撤回，此限制需纳入机构政策。

完整系统备份使用独立运维权限和存储，不通过普通导出按钮提供。打印继续使用 `PaperForm` 和 `window.print()`，只输入授权范围内的建筑及跟进参考；纸本编号由 D1／D8 确认的方式管理，不默认依赖随机会话 ID。

## 9. 候选最小 API 与现有前端的连接

以下 `/api/v1` 路径是建议契约。主系统已有等效接口时可适配；D4、D5 尚未选定。所有业务接口检查负责人会话，不在本期加入多机构或按工作员／上传者隔离的数据规则。

| 方法／路径 | 目的及返回 | 当前或拟新增调用点 |
| --- | --- | --- |
| `GET /session` | 身份、有效权限和必要范围信息 | `session.ts`、`SessionGate`；实际登录／退出方式按 D4 决定 |
| `GET /buildings?query=&cursor=&limit=` | 建筑列表及覆盖／跟进摘要 | `App` 侧栏；限制分页大小 |
| `GET /buildings/{id}` | 单幢详情、已声明布局及摘要 | `BuildingDetail`；不返回全机构个人数据 |
| `GET /observations?buildingId=&floorId=&unitId=&cursor=` | 指定范围历史与来源、精度信息 | `ObservationHistory`；顺序和分页规则固定 |
| `GET /follow-ups?buildingId=&cursor=` | 未关闭跟进、原观察引用及必要展示信息 | 待跟进筛选、详情和打印 |
| `GET /buildings/{id}/paper` | 纸本预览所需的最小授权数据 | `PaperForm`；不要求返回 PDF |
| `POST /imports` | 上传 XLSX，自动识别候选格式／字段，返回批次 ID 和状态 | `ImportDialog`／远程 repository |
| `GET /imports/{id}` | 候选映射和解释、识别状态、预览版本、分页问题或提交结果 | 导入预览、刷新恢复、提交超时查询 |
| `POST /imports/{id}/mapping` | 确认／修正字段映射，重新解析并生成新预览版本 | 新增映射确认区；不等于实体匹配或批准写库 |
| `POST /imports/{id}/matches` | 提交已知实体的人工目标匹配，产生新预览 | D8 未定时不开放无基线主数据更新；不能靠姓名自动匹配 |
| `POST /imports/{id}/confirm` | 预览版本和幂等键；返回原子提交结果 | `confirmImport`；禁止直接提交整个 snapshot |
| `POST /exports` | 申请授权范围内的回录工作簿，建立导出基线 | 现有 Excel 导出按钮 |
| `GET /exports/{id}/download` | 重新授权后下载已生成文件，或明确返回未就绪／过期状态 | 下载入口 |
| `GET /health/live`、`GET /health/ready` | 进程存活与必要依赖就绪 | 部署监测；不泄露数据库配置和业务内容 |

如果正式模式保留中心补录表单，再增加 `POST /observations`，复用导入提交所用的领域命令、授权、幂等及事务，不做第二套规则。第一期不必须给全部主数据开放 `PATCH`／`DELETE` API。

地图不应依赖当前列表的一页数据而漏掉其他建筑。可增加受限 `GET /map/buildings?bbox=` 返回授权范围的轻量几何／摘要，或在小规模试点中独立读取完整的轻量地图集合；不能把分页列表误当成地图全量。

错误返回建议统一为 `code`、安全可读的 `message`、`requestId` 和必要字段问题，不返回原始数据库错误或未授权记录。主要语义：

| 状态 | 前端应有行为 |
| --- | --- |
| `401` | 提示重新认证，停止后续写入；敏感缓存和草稿按会话政策处理 |
| `403` | 明确无权限，不静默改为本地保存，不回显不可见数据 |
| `409` | 显示过期预览、版本冲突、重复结案或幂等键冲突，要求重新核对 |
| `413`／`422` | 展示资源限制或工作表／行／字段问题，保留可继续核对的输入 |
| `429`／`503`／网络超时 | 显示可重试状态；写入先查批次或复用幂等键，不自动重复提交新事件 |

## 10. 现有前端具体需要怎么改

### 10.1 repository 和 store 不能只换 URL

[repository.ts](src/data/repository.ts) 的 `PersistenceAdapter.read/write` 是同步接口，`replaceSnapshot` 是整份覆盖。[store.ts](src/app/store.ts) 直接实例化本地 repository，并在调用后立即发布成功状态。它们不能不改调用语义就适配网络。

需要的改造：

1. 保留 demo 的本地实现，新建具有 Promise 返回值的业务 repository 接口及远程实现，不让 HTTP 适配器伪装成同步 `Storage`。
2. 将初始化、加载详情、导入确认、导出以及获准保留的中心补录改为异步 action；添加独立 loading／error／request 状态。
3. `App.confirmImport` 必须等待确认完成再关窗和显示成功。`saveObservation` 若保留，也必须等待结果，不能沿用当前立即提示成功的路径。
4. 服务端提交后按返回的受影响建筑重新读取摘要、历史及跟进；失败不清除正在核对的内容，不把旧缓存伪装成已保存结果。
5. 选择建筑和镜头状态继续由 Zustand 管理；快速切换时取消过时读取或用请求身份丢弃旧响应，防止 A 建筑数据覆盖 B。
6. 正式模式退出／过期时清理相应身份的敏感缓存，不把正式工作区自动保存到原 localStorage。弱网不自动切换为离线写入，因为离线同步不在范围内。

### 10.2 组件层的必要衔接

| 现有位置 | 必需或条件性变化 |
| --- | --- |
| `App.parseFile` | 正式模式交给服务端权威预览；前端可先检查扩展名／大小，但不能把客户端合并当作提交依据 |
| `ImportDialog` | 增加候选格式、表头行、自动映射解释和人工修改区；映射改变使旧预览失效；接入批次、冲突及分页问题；显示行级分类（`READY`／`NEEDS_REVIEW`／`INVALID`）、疑似重复分组与批次失效倒计时；提供「补齐后整批提交」与「只提交可提交行」两个显式入口，不提供静默略过问题行的操作 |
| `App` 的示范工作员及 Visit ID | 正式路径的 D2 纸本信息可空，不以登录者覆盖原工作员；未知外出编号留空，不用浏览器会话伪造业务批次 |
| `ObservationHistory` | 当前建筑模式包含所有无 `unitId` 记录；正式范围查询须区分建筑／楼层，显示必要的位置和时间精度，不能把楼层结果误认成建筑结果 |
| `BuildingDetail` | 已有楼层选择但没有独立楼层新增入口；优先保证 Excel 楼层记录可看、可跟进，不默认新建编辑页面 |
| `PaperForm` | 输入从完整 snapshot 逐步改为授权的打印视图模型；保留 A4 版式和已知纸本流程 |
| `App.exportExcel/exportData` | 正式模式调用受控导出，不能对浏览器全量缓存直接生成封存／JSON；演示导出可保留 |
| `MapScene` | 保留渲染、镜头和现有防遮挡行为，只接入轻量地图数据；不让观察编辑触发不必要的镜头重置 |
| `App`／帮助／状态栏 | 正式模式不再显示“本机保存”“示范工作员”等错误状态；演示模式继续显示明确演示标记 |

不要为了上述衔接强行维持完整 `OutreachSnapshot`：详情、历史和打印可逐步使用小型视图模型。负责人拥有全量业务可见性，但“可见全部”不要求首次打开地图就一次下载全部个人资料；按需加载和分页只是性能设计，不是数据隔离。

## 11. 流畅运行、部署和数据生命周期

### 11.1 性能重点

当前多处使用全量 snapshot 扫描，保存时还会重新解析、校验并序列化整个对象；中文工作簿先完整解析，旧格式回退时再解析一次。函数标记为 `async` 不会让 SheetJS 的同步解析自动离开主线程。

第一期应做：统一受限解析入口、服务器权威预览、分页历史／问题列表、轻量摘要、稳定查询索引、取消过时请求，以及保存后只刷新受影响范围。避免每次查询跟进都从浏览器全量重算。

候选索引围绕实际查询建立：观察的位置和时间／ID排序、布局父子关系、非空结案引用、导入批次状态、幂等键。本期不增加多租户索引和隔离规则。是否引入预计算汇总、额外缓存和工作队列应以实际数据量及测量结果为依据。

底图仍依赖网络和 WebGL。需要继续保留清单和打印降级，但不能把这种降级描述为离线业务系统或离线地图缓存。性能验收须先约定试点数据量、设备和网络条件，不承诺未经测量的响应秒数。

### 11.2 部署补充

独立服务方案下建议补齐：

- 前端构建产物、业务 API、关系数据库和受控文件存储的清晰边界；生产 HTTPS，同源反向代理优先。
- 服务端启动时校验必要配置，提供迁移执行、就绪检查、优雅停机和连接管理；缺失数据库时不以成功状态启动业务。
- 如采用 Cookie 会话，明确 HttpOnly／Secure、过期、登出、CSRF 及来源检查；跨域场景明确允许来源，不以任意来源加凭证访问。
- `DATABASE_URL`、会话密钥、文件存储凭证只用于服务器；`VITE_` 变量对浏览器公开，不放密钥。前端 API base 可以是公开配置。
- 请求 ID、结构化错误、导入失败／冲突率、数据库与备份状态监控；错误日志不得包含整份 Excel 或完整个人资料。
- 基于已选托管环境的发布、迁移和回退说明；代码回退不能等同于数据库回滚，迁移需避免破坏旧数据。

若接入主系统，应使用其等效设施并记录负责人，不机械增加第二套部署和账号服务。[.openai/hosting.json](.openai/hosting.json) 目前仅指向静态 `dist`，自身不能运行这些服务。

### 11.3 备份、恢复与删除

用户将 D7 委托给本方案，采用下列**试点工程默认值**，后续可配置调整；不是已验证的 SLA 或法定保留期限，本轮也不执行删除或备份操作。

| 对象／能力 | 默认方案 | 约束与执行方式 |
| --- | --- | --- |
| 上传的原始 XLSX、单元格快照和映射修正材料 | 接收后最多保留 30 天；未提交批次 7 天失效 | 存放非公开受控存储；到期删除文件及含原值的中间副本，不删除已提交业务事实；延长须显式批准并记录；失效后待核对行的取回方式见第 8.2.1 节 |
| 临时导出文件 | 24 小时失效并清理 | 每次下载验证负责人会话；用户已下载的离线副本不能撤回 |
| 服务端可信导出基线 | 90 天有效 | 与 24 小时下载文件独立；基线过期必须重新导出／核对，不接受客户端封存冒充可信基线 |
| 调试／运行日志 | 30 天 | 不写姓名、电话、原行及整份请求；按 request ID、批次 ID 排障 |
| 最小操作审计 | 滚动保留 180 天 | 记录操作者、操作类型、时间、对象 ID 和结果；不复制整个观察原文或 Excel |
| 已提交业务记录及必要来源、去重身份 | 试点期间不因上述临时文件到期而自动删除；每 90 天进行一次必要性复核 | 不设无限保留承诺；不再需要或收到有效删除要求时走受控删除／去标识流程，不能只隐藏界面 |
| 幂等及批次提交证据 | 与相关业务记录生命周期一致 | 可清理响应正文，但保留最小请求摘要、业务键及提交引用；不能因清理原文件而令重传重复入库 |
| 数据库备份 | 每日一次加密备份，滚动保留 30 天；迁移前另作可恢复备份，同样设期限 | 数据库、必要文件和提交证据有一致恢复点；备份与主实例分开存储，密钥不在业务库或前端 |
| 恢复目标 | 初始目标 RPO 不超过 24 小时、RTO 不超过 1 个工作日 | 是工程目标，选定 D5 后用真实体量演练；达不到时不得宣称满足，须调整资源／方案 |
| 恢复演练及告警 | 首次真实试点前、之后每月及重大迁移后演练；每日检查备份结果 | 失败显式告警；D5 落定时指定实际运维负责人，不能只有无人接收的日志 |
| 恢复权限 | 默认不通过普通负责人业务页面直接恢复数据库 | 运维执行，负责人确认恢复点和影响；共享主系统时适配其等效流程，不另造运维平台 |

删除／去标识采用“说明原因和范围 → 展示受影响关系 → 负责人确认 → 受控执行 → 最小审计”的流程；历史更正仍优先追加，不静默篡改旧事实。删除请求还覆盖原文件、中间结果、导出和未来训练素材；保留必要的非敏感删除标记，恢复旧备份时先重放删除／去标识决定，再开放访问。

不可修改的备份通常随 30 天窗口到期移除，不承诺即时逐条擦除；必须限制恢复并防止已删资料重新进入服务。如果实际适用规则要求更短期限或即时删除，应调整备份策略而非假称合规。临时数据清理失败必须告警；保留身份键的去重证据也应纳入同一生命周期管理。

恢复演练检查关系、历史、已关闭跟进、导入提交和幂等结果，不能只确认数据库能启动。D4／D5 尚未确定不妨碍实现这些可配置策略，但实际生产运行须有选定环境、密钥及责任人。

### 11.4 开发工具的可复现性

`npm run demo:generate` 只调用 `scripts/generate-demo.ts`，重建旧英文样例，并不重建全部三份工作簿。

街区美化生成脚本依赖外部 artifact runtime 和 `@oai/artifact-tool`，不是当前 `package.json` 声明的常规安装依赖。几何提取脚本还需要明确来源的瓦片输入，其直接使用的包应与工具链声明保持一致。普通前端使用已提交的样例，不依赖每次运行这些脚本。

后续若要求可复现生成，应复用已声明的 SheetJS 或单独固定生成工具链及输入来源；这不是第一期共享导入闭环的默认前置条件，不应为了生成报告而重建资源。

## 12. 可拆分的实施工作包

D1、D3、D6 已有当前方案，D7 已制定默认值；D2、D8 刻意留空，不再要求“D1–D8 全部答完才能动工”。D4、D5 保留双路径适配，在联机发布前落实。此处不提供未经确认的工期承诺。

| 工作包 | 具体交付及代码范围 | 前置条件 | 验收与完成标准 |
| --- | --- | --- | --- |
| W0：候选格式与识别接口 | 见第 8.1.2 节：固定候选格式清单、表头别名与位置容错规则、识别输出与映射预览约定、三类回归样例 | 现有格式和本轮决定；不等待训练集 | 候选格式清单冻结且数量有限；每个格式都有成功／歧义／拒绝识别样例；已知／候选／歧义／未知可区分，规则可替换；不做模型训练；D2、D8 不被擅自补齐 |
| W1：正式数据与共享规则 | `shared/contracts`、必要 domain 提取、demo／正式版本分离、可空纸本字段和旧字段保留；第 7.4 节的有效版本与更正链契约 | W0 的候选契约；不依赖 D4／D5 | 日期和未知值不伪造，角色可空；只在信息足够时进入业务计算；更正生效规则定义完成且可测试 |
| W2：负责人访问与共享持久化 | 身份适配接口、单机构全量业务访问、数据库约束、事务、审计及 D7 清理／备份策略 | W1；**接入真实登录前选定 D4，第一次真实数据库迁移前选定 D5**，此前只保留一条可运行路径 | 不做人员行级隔离，未认证访问仍被拒绝；关系约束和持久化有效；选定的实作路径已记录 |
| W3：读取、打印和异步前端 | 列表／详情／历史／跟进／地图／打印读模型和空状态 | W2；合成验证不依赖 D8 | 负责人看全机构数据；空库不注入假资料；正式初始化及其验收等 D8 后补 |
| W4：自动识别与导入闭环 | detector／profiles、候选映射确认、两类 parser、批次／事务／去重／冲突；第 8.2.1 节的行级分类与拆批提交；第 8.3 节的去重前提；接 `ImportDialog` | W0、W1、W2 和展示入口 | 普通表无需封存；歧义可纠正；D2 空值不阻塞；缺关键目标／日期留待核对且不部分入库；拆批只由负责人显式触发；无业务键时只提示疑似重复 |
| W5：再导出与历史保护 | 负责人全量／自选范围导出、可信基线、下载授权和到期 | W2、W4、当前 D6 和 D7 默认值 | 全量可导出，单幢导出不夹带全量封存；24 小时文件与 90 天基线分别到期；删行不删历史；更正后的有效版本随导出一致输出 |
| W6：故障、并发与恢复准入 | 数据库并发测试、重试和重复结案验证、恢复演练、试点设备性能、发布回退说明 | W2–W5；**D5 必须在演练前选定，不允许在适配层上声称演练完成** | 第 13 节场景达到约定；权限、恢复、设备和真实数据政策具备签认条件；演练在已选定的真实环境上进行 |
| W7：按需管理功能 | 由真实试点阻塞决定的人员／家庭／居住／会员／布局最小维护入口 | 已证明导入或受控维护无法满足需要 | 每项具备实际使用者和独立验收，不扩大成全套平台 |

W4 与 W5 的格式契约必须在 W0／W1 一起设计，不能先写完导入器再临时决定导出基线；只有实现和联调可按工作包分批完成。

**关于 D4／D5**：未定不阻塞 W0 和第 7、8 节的契约设计，但每个工作包的前置条件都收窄为「保留接口、只跑一条路径」。第一次真实数据库迁移（W2）、真实登录接入（W2）和恢复演练（W6）开始之前，必须先选定一条实作路径并记录下来；预留适配接口不等于长期同时维护两套实现，任何一项开工时若仍未选定，应先补齐该决策再动手。

实际动工时，每个工作包继续拆为“schema／服务端／前端／用例／运行配置”任务，并保留依赖关系。先完成一个可演示的共享纸本／Excel 纵向流程，不以页面数量衡量第一期完成度。

后续训练集工作独立排期：取得授权和标注 → 固定评估集 → 接入模型 detector → 与规则版比较 → 回归后发布版本。OCR 不随训练集接入自动纳入范围。

## 13. 验收场景

以下是后续实施的验收要求，不是本次已经通过的结果。现有 domain、repository、Excel 和地图用例应保留；新增服务端并发与授权用例需要真实数据库或等效真实约束环境，不能仅用内存 Map 模拟成功。

| 编号 | 场景 | 必须得到的结果 |
| --- | --- | --- |
| A1 | 两位负责人接续同一纸本／Excel 工作 | 第二位能查看第一位导入的数据和批次，并继续确认／导出；不按上传者或人员归属隔离 |
| A2 | 纸本工作员、回录者或原时间缺失 | D2 字段留空，不被登录者／服务器时间替代；仍记录实际上传／确认者；不启动 OCR |
| A3 | 原表只填日期、时间未知或写“翌日” | 保留精度和原话，不制造时刻、时区或确定期限；原始信息可追溯 |
| A4 | 普通机构 Excel 没有封存、表头有别名或位置变化 | 自动给出候选格式／映射及解释；歧义可人工修改，关键问题阻止写库；不创造身份或覆盖资料 |
| A5 | 相同纸本改名、排序或重复上传 | 有稳定业务键时：同一业务事件不重复，同键不同内容报 `409` 冲突；无可靠业务键时：只显示疑似重复并要求负责人核对，不自动合并也不自动新增；两次合法探访不被误合并 |
| A6 | 预览后其他同事修改目标资料 | 确认发现版本变化，不覆盖较新内容；用户可重新预览和处理冲突 |
| A7 | 两人同时关闭同一跟进 | 最多一个有效结案，另一请求明确冲突；旧开放事件及结案历史都保留 |
| A8 | 确认超时或用户反复点击 | 使用同一请求／批次查询可得到一致结果；不会产生部分导入或重复观察 |
| A9 | 删除旧 Excel 行、导入旧版本文件 | 不删除已有记录，不倒退较新历史；不可改写的观察被明确保护 |
| A10 | 单位结果和楼层记录进入建筑摘要 | 范围准确；单个单位不能把整幢标记完成；未知总量不生成虚假完成比例 |
| A11 | 负责人会话失效／撤销，或同机构另一负责人访问批次 | 前者拒绝访问；后者可接续处理并记录操作者，不错误套用“只能访问自己的数据” |
| A12 | 负责人选择全量或单幢导出 | 全量包含所需全机构业务数据；单幢的全部页／封存／JSON 限于所选范围；不含凭证或备份密钥 |
| A13 | 地图网络失败或 WebGL 不可用 | 已有清单、详情、打印及中心回录流程仍可使用；不宣称完全离线可用 |
| A14 | 空库、服务重启、账号退出 | 空库提示待初始化，不擅自决定 D8；已有记录不丢；登出清会话缓存；正式初始化验收后补 |
| A15 | 恢复与过期清理 | 演练目标 RPO 24 小时／RTO 1 个工作日；恢复后重放删除决定；30 天原文件清理不破坏业务去重 |
| A16 | 试点约定体量的普通表／大表／无效表 | 有资源上限和可理解的失败结果；预览、列表和地图性能按设备与网络条件记录 |
| A17 | 未知表、歧义表、图片／扫描件 | 未知或歧义不伪装识别成功；提供修正或退回路径；图片明确提示暂不支持 OCR；第 8.1.2 节清单内每个候选格式都有成功／歧义／拒绝识别三类样例，且结果可复现 |
| A18 | 后续训练模型替换规则识别 | 固定输入／输出及版本；回归样例不退化；不自动重写旧批次，不把生产文件自动用作训练数据 |
| A19 | 把错填的未来日期更正为较早日期（只追加一条更正） | 原事件保留可追溯但退出覆盖计算；覆盖与摘要按更正后的日期重算，原错误事件不再被当作最新；历史同时显示两条并标出当前有效版本；跟进变化有显式记录；导出携带更正链摘要 |
| A20 | 100 行中有 1 行缺日期 | 该行进入 `NEEDS_REVIEW` 而非被略过；负责人可选择补齐后整批提交，或显式拆出有效批次、重新预览后提交；系统不自动略过问题行、不产生部分导入；批次 7 天失效后按第 8.2.1 节取回待核对资料，已提交部分不受影响 |

真实数据试点仍需选定 D4／D5（选定不得晚于第一次真实迁移、登录接入与恢复演练）、具备实际资料来源和可执行的字段／目标核对方式、关键场景通过及恢复路径有人负责。D2 留空是允许状态，不要求重新补完所有纸本身份；D8 的真实初始化不能靠演示资料替代。演示标记、静态站点私有访问或一次成功识别不能替代这些条件。

## 14. 当前可执行的下一步：一个有边界的 W0

**先把 W0 做成可独立验收的交付，再推进 W1–W5 的单机构负责人闭环。** 本轮不再扩写总体规划；W0 交付下面三项即算完成，验收通过后才动共享数据闭环。

1. **固定候选格式清单**：只支持第 8.1.2 节清单内的少量格式，清单冻结并版本化，新增格式属于下一版。每个格式记录来源、表头特征、必需字段和已知位置变化。
2. **映射预览**：按第 8.1.1、8.1.2 节输出候选表头行、字段映射、匹配依据和 `KNOWN / CANDIDATE / AMBIGUOUS / UNKNOWN` 状态，允许人工修正；修正后重新解析并产生新的预览版本。
3. **回归样例**：每个候选格式至少三份——成功识别、歧义（必须人工选择）、拒绝识别（明确不支持并给出原因）；样例为合成资料、随代码提交、可反复运行。

**W0 验收**：清单内格式的识别结果可复现；歧义不被伪装成成功；拒绝识别有明确提示和退回路径；固定输入／输出接口可替换识别器；没有引入模型训练、外部 AI 调用或 OCR；D2、D8 没有被擅自补齐。

W0 的输入、输出与第 8.2、8.2.1、8.3 节的批次契约一起设计，但**不要求 W0 就实现事务、登录或数据库**。W1–W5 按第 12 节的前置条件推进；D4／D5 的选定不得晚于第一次真实数据库迁移、登录接入和恢复演练，起跑前保留接口、只跑一条路径。

当前可用合成样例开发识别规则，正式处理每份未知表仍须预览核对。训练集后续只用于改进识别模块；OCR、数据隔离平台及完整管理页面不在本期。上述内容仍是实现计划，本轮只改文档，没有启动代码开发。
