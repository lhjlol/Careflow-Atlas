# Optional replacement for the public OpenFreeMap vector style.
# Map requests go directly to this provider. Never put secrets in VITE_ variables.
# VITE_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/positron
