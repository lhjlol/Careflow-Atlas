# CLAUDE.md — CareFlow Atlas 项目工作约定

> 本文件是 CareFlow Atlas 仓库内的工作约定，由 2026-09-13 对 `BACKEND_GAP_ANALYSIS_副本.md` 的两轮评审经验整理而成。
> 计划全文以 [BACKEND_GAP_ANALYSIS_副本.md](BACKEND_GAP_ANALYSIS_副本.md) 为准；本文件只固化「动工前必须遵守什么」，不复制计划内容。

## 1. 项目当前状态

- 这是一个**合成资料的纸本／Excel 外展原型**：React + Zustand + SheetJS + MapLibre，数据落在浏览器 `localStorage` 的整份 snapshot。
- 仓库里**没有后端**：没有 HTTP API 服务、没有数据库、没有服务端启动或迁移脚本。`fetch` 只用于下载演示工作簿和地图底图。
- 领域、解析、合并、追加式历史、打印、地图已经存在；缺的是「多人共享同一份可信数据」的闭环。
- 所有现有测试夹具都是合成数据，`isSynthetic` / `schemaVersion: "0.1-demo"` 是演示标记，**不是正式 schema 版本**。

## 2. 已确定的边界（不要再重新讨论）

| 项 | 决定 |
| --- | --- |
| 机构范围 | 单一机构；负责人可看到全机构业务数据。不做多租户、部门或按人员的行级隔离 |
| 仍然保留 | 登录、会话、操作审计、运维权限；未认证访问必须被拒绝 |
| 演示与正式 | 必须分开。正式模式不加载演示数据，演示标记不得进入正式库 |
| 明确排除 | OCR／图片识别、手机现场记录、离线同步、自动医疗或法律判断、未经确认的政府数据集成 |
| 训练模型 | 本期不做。不调用外部 AI 上传机构文件 |

D2（纸本角色／原始时间）与 D8（初始资料来源）**刻意留空**，不要擅自补齐或用演示资料冒充。
D4（账号）／D5（部署）保留选择，**但只在接口层保留**——见第 5 节。

## 3. 四条硬规则（踩过的坑，不要再犯）

### 3.1 W0 的支援范围必须封闭

自动识别不能变成通用 Excel 识别平台。第一版只支持计划第 8.1.2 节清单内的少量候选格式（中文六表封存、旧英文合成、一人一行、一幢建筑一行、一处一次探访一行），支持表头别名、位置变化和人工修正即可。

- 每种格式必须有**成功 / 歧义 / 拒绝识别**三类回归样例，缺一不算完成。
- 超出清单的格式明确拒绝识别并给出原因，不猜、不静默丢弃列。
- 不以「支持更多格式」作为验收方向；不训练模型。

### 3.2 去重承诺必须带前提

去重能力取决于业务键是否可靠，**不能把「不重复」写成无条件承诺**。

- **有稳定业务键**（命名空间内稳定 ID，或机构确认唯一的纸本编号／行号／位置组合）：保证去重；同键内容一致视为重复，同键内容被改写报 `409` 冲突。
- **没有可靠业务键**：只显示**疑似重复**并要求人工核对；不得仅凭内容相同自动合并，也不得仅凭内容相同就断定是两次不同探访而直接新增。
- 内容相同既不足以判定「同一次探访」，也不足以判定「两次合法探访」。判定结果要留核对记录，作为后续重传的去重依据。

### 3.3 待核对行不阻塞整批，但也不许被静默略过

「缺日期／目标的行进入待核对」与「整批原子提交」并不冲突，关键是**拆批由负责人显式决定**：

- 行级分类：`READY` / `NEEDS_REVIEW` / `INVALID`，预览可见可筛。
- 两条路径：**补齐后整批提交**（默认）或**负责人主动拆出有效批次、重新预览后提交**。
- 禁止实现自行跳过问题行、用默认日期或默认目标补齐、把部分写入包装成整批成功。
- 未提交批次 7 天失效，要在界面上显示倒计时，并交代待核对资料如何取回（原文件在 30 天内可重新上传重新预览；已提交部分不受影响）。

### 3.4 「追加更正」必须真的生效

只追加一条更正记录、却仍让原事件参与最新值比较，**等于更正没有生效**。当前 [compareObservationTime](src/domain/types.ts#L74)／`latestObservation` 按「发生时间 → 录入时间」取最新，正是这个缺陷：把错填的未来日期改成较早日期后，原错误事件仍会被当成最新。

- 原事件：不改写、不删除，保留追溯。
- 更正记录带 `correctsObservationId` 指向原事件，本身作为新的观察事件参与比较（按更正后的 `occurredAt`）。
- **被更正的原事件退出覆盖／摘要候选集**，但保留在历史中；更正链链尾生效；并列冲突报 `409` 要人工判定。
- 覆盖与摘要按有效版本重算；历史同时显示两条并标出当前有效版本；跟进以有效版本为准，变化要有显式记录，不得让待跟进项目静默消失。
- 引用到 [getCoverageStatus](src/domain/types.ts#L81)、[getCoverageSummary](src/domain/types.ts#L91) 的所有位置都要一起改，不能只改一处。

## 4. 数据与时间规则

- **角色与时间分开**：现场工作员、回录者、上传者、确认者是四个不同的东西。不用登录者替代纸本工作员，不把服务器时间当成纸本时间。
- 缺失就是缺失：未知值留空、原单元格保留，**不推断、不补造时分秒、不生成确定期限**。只填日期的探访保持日期精度。
- 旧字段（`workerName`、`recordedAt`）保留为 legacy 值并注明原语义，不回填不存在的认证主体。
- 观察事件原则上只追加；范围引用必须一致；覆盖、接触结果、住房判断相互独立。
- 名称／同址／同电话不是唯一身份；不得据备注凭空制造楼层、单位、坐标或人员。

## 5. D4／D5 的决策闸门

未定不阻塞 W0 和契约设计，但下面三件事**开工前必须先选定一条实作路径并记录下来**：

| 闸门 | 最迟决策点 |
| --- | --- |
| D4（账号来源） | W2 接入真实登录之前 |
| D5（部署与数据库） | W2 执行第一次真实数据库迁移之前 |
| D5（恢复演练） | W6 演练之前；不允许在适配层上声称演练完成 |

预留接口 ≠ 同时实现两套。同一时间只维护一条可运行路径，另一条只保留接口定义与最小冒烟验证。

## 6. 代码约定与验证

改动前先跑基线，改动后必须复跑：

```bash
npm run typecheck   # tsc -b --pretty false
npm run test        # vitest run，12 个文件 / 60 个用例
npm run lint        # eslint .
npm run dev         # vite --host 127.0.0.1
```

- 既有测试断言的是演示语义，不要为了让新代码通过而放宽它们。
- 新增领域规则优先放进 `src/domain/`，浏览器无关；不要复制第二套与现有规则不同的逻辑。
- 正式路径要改成异步时，不要假装 HTTP 适配器是同步 `Storage`：`PersistenceAdapter.read/write` 是同步整份快照接口，正式 repository 另建 Promise 接口。
- 提交前检查 `git status`：**不要覆盖、还原或顺带格式化镜头防遮挡修复**等不属于本次任务的改动。
- 分析文档提到的 checkout 可能与本机工作目录不同（本轮为 `/Users/lhj/Desktop/Light/洗楼系统/Careflow-Atlas`，`main` @ `8127ef3`）；动手前先确认 diff 归属。

## 7. 不要做的事

- 不要在没有业务键的情况下承诺去重、不要用文件名或摘要当业务身份。
- 不要把格式识别当成身份匹配或自动批准入库。
- 不要为了「看得到效果」而在正式模式注入演示建筑或伪造完成比例。
- 不要把普通用户导出完整 JSON 当作备份制度；备份凭证不进业务库和前端。
- 不要把待核对清单、正式导入文件、导出文件或备份放进 `public/` 等公开静态目录。
- 不要在 W0 阶段就引入事务、登录或数据库实现。
